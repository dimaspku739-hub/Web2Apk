    name: Build APK
    on:
      workflow_dispatch:
        inputs:
          release_tag:
            description: 'Tag release yg mau di build'
            required: true
    
    jobs:
      build:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout
            uses: actions/checkout@v4
          
          - name: Download ZIP dari Release
            run: echo "Download ${{ inputs.release_tag }}"
          
          - name: Build APK
            run: echo "Proses build disini"
